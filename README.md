# UNDER CONSTRUCTION

Disclaimer: This application is being actively developed in a pre-release phase, it probably looks like straight crap and don't work too good.

# Invaders TS

_As this is a hobby project written because I like to code, no LLMs were used to generate anything in this repository.
All code, assets, etc. were all created by hand.
LLMs were used to reference library documentation, find bugs, etc._

Reimplementation of Taito's _Space Invaders_ using HTML5 canvas and Typescript.
Just for kicks and since I haven't done an HTML5 canvas app in a while.
_Space Invaders_, being quite a simple game, is a good test drive project for different frameworks/stacks.

# Run Debug Server

The following command runs a local debug server with live (not hot) reloading.

```
npm run dev
```
